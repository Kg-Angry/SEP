server.port=8081
spring.application.name=koncentrator-placanja

eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/

#eureka.instance.nonSecurePortEnabled=false
#eureka.instance.securePortEnabled=true
##eureka.instance.securePort=${server.port}
##eureka.instance.statusPageUrl=https://${eureka.instance.hostname}:${server.port}
###eureka.instance.healthCheckUrl=https://localhost:${server.port}/health
###eureka.instance.homePageUrl=https://localhost:${server.port}
#eureka.client.enabled=true

##BEZBEDNOST
## The format used for the keystore. It could be set to JKS in case it is a JKS file
#server.ssl.key-store-type=PKCS12
## The path to the keystore containing the certificate
#server.ssl.key-store=keystore.p12
## The password used to generate the certificate
#server.ssl.key-store-password=kgangry
## The alias mapped to the certificate
#server.ssl.key-alias=tomcat
#
#server.ssl.client-auth=none
#
#server.ssl.enabled=true